# Iron-session cookie encryption secret. Required.
# Must be at least 32 chars. Generate with: openssl rand -base64 32
# Rotating this value invalidates every existing session cookie (all users get logged out).
SESSION_SECRET=
